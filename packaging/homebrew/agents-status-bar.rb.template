cask "agents-status-bar" do
  version "VERSION"
  sha256 "SHA256"

  url "https://github.com/GITHUB_OWNER/agents-status-bar/releases/download/v#{version}/AgentsStatusBar-#{version}.zip"
  name "Agents Status Bar"
  desc "Menu bar usage monitor for local AI coding agents"
  homepage "https://github.com/GITHUB_OWNER/agents-status-bar"

  depends_on macos: ">= :sonoma"

  app "Agents Status Bar.app"

  zap trash: [
    "~/Library/Preferences/dev.agentsstatusbar.app.plist",
  ]
end
